package skill

// BuiltinDefinitions returns skills shipped with Foya. Their instructions are
// embedded in the binary so the default installation remains self-contained.
func BuiltinDefinitions() []Skill {
	return nil
}
